{
  "name": "dream-reveal-letter",
  "version": "1.0.0",
  "description": "Full-stack personalized dream reveal letter app with admin panel, uploads, dynamic links, and Supabase PostgreSQL persistence.",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check script.js && node --check admin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  }
}
